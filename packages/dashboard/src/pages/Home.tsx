import { Typography } from 'antd';

export default function HomePage() {
  return (
    <div>
      <Typography.Title level={2}>全局概览</Typography.Title>
      <Typography.Paragraph>Dashboard 首页 — 即将实现</Typography.Paragraph>
    </div>
  );
}
